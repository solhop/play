on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - run: npm test -- --firefox
      - run: npm test -- --chrome
      - run: npm test -- --safari
